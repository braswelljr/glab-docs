# Introduction

__ GLab __ is an open source _GitLab CLI Tool_ written in Go(Golang) to seamlessly work with GitLab from the command line. Work with issues, merge requests, watch running pipelines directly from your CLI among other features. Inspired by `gh`, the [official GitHub CLI tool](https://github.com/cli/cli).

```js
console.log('hello')
```